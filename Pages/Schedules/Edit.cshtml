@page "{id:int}"
@model WebApp.Pages.Schedules.EditModel
@{
    ViewData["Title"] = "Edit Schedule";
}

<h1>Edit Schedule</h1>

<div class="row">
    <div class="col-md-6">
        <form method="post">
            <input type="hidden" asp-for="Id" />
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="mb-3">
                <label asp-for="Input.Name" class="form-label"></label>
                <input asp-for="Input.Name" class="form-control" />
                <span asp-validation-for="Input.Name" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Input.Color" class="form-label"></label>
                <select asp-for="Input.Color" class="form-select">
                    @foreach (var color in Enum.GetValues<WebApp.Models.FolderColor>())
                    {
                        <option value="@color">@color.ToDisplayName()</option>
                    }
                </select>
            </div>

            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-page="/Schedules/Index" class="btn btn-outline-secondary">Cancel</a>
        </form>

        <form method="post" asp-page-handler="Delete" class="mt-3" onsubmit="return confirm('Delete this schedule? Notes and folders linked to it are kept, just unlinked.');">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-outline-danger">Delete Schedule</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
