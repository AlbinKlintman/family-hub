name: CI/CD

on:
  push:
    branches: [main]

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/albinklintman/family-hub
  DEPLOY_DIR: /home/albin/projects/family-hub/WebApp

jobs:
  build-and-deploy:
    runs-on: [self-hosted, homelab]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # No actions/setup-dotnet: this is a persistent self-hosted runner
      # with the .NET SDK already installed at the system level.
      - name: Restore
        run: dotnet restore WebApp.slnx

      - name: Build
        run: dotnet build WebApp.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test WebApp.slnx --configuration Release --no-build

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Deploy
        run: |
          cd "$DEPLOY_DIR"
          git fetch origin main
          git reset --hard origin/main
          docker compose pull
          docker compose up -d --remove-orphans
